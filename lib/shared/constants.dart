import 'package:flutter/material.dart';

const textInputDecoration = InputDecoration(
  fillColor: Colors.white,
  filled: true,
  enabledBorder: OutlineInputBorder(
      borderSide: BorderSide(color: Colors.white12, width: 2.0)),
  focusedBorder: OutlineInputBorder(
      borderSide: BorderSide(color: Colors.lightBlueAccent, width: 2.0)),
);

final Color backgroundColor = backgroundColor;
const c = Color(0xFFD6EAF8);

final Color appBarColor = backgroundColor;
const a = Color(0xFF2980B9);

final Color buttonColor = backgroundColor;
const b = Color(0xFF2980B9);
